package com.weather.pogodynka.service;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.BDDMockito.given;
import static org.mockito.Mockito.mock;

class GeocodingTest {

    /*@Test
    void getDestinationTest() {
        //given
        Geocoding geocoding = mock(Geocoding.class);

        String someRandomSecretKey = "xyz00f7feeb86d6511a8bce5d0bd3abc";
        String exampleCorrectUserInput = "Tokio";
        String realSecretKey = "68400f7feeb86d6511a8bce5d0bd3036";

        given(geocoding.getDestination(realSecretKey, exampleCorrectUserInput)).willCallRealMethod();

        //when
        String result = String.valueOf(geocoding.getDestination(realSecretKey, exampleCorrectUserInput));

        //then
        assertThat(result).contains("local_names");
    }*/
}